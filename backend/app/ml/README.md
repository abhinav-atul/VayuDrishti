# ML model artifacts are generated at runtime
# Run POST /api/model/train to generate:
# - model.joblib (trained XGBoost model)
# - validation_results.json (LOSO cross-validation metrics)
# - shap_importance.json (feature importance scores)
